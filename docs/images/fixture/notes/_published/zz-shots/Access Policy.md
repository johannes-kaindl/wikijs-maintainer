# Access Policy (current)

The current access policy. A second note (`access-policy.md`, lowercase and
hyphenated) slugifies to the same wiki path — both are shown blocked in the
status view until one is renamed.
